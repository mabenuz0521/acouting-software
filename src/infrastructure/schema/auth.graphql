type Token {
  token: String
  user: UserResponse
}

type Query {
  login(email: String!, password: String!): Token
  greetings: String!
  greeting: String!
}

type Mutation {
  singup(email: String!, password: String!): UserResponse
}
