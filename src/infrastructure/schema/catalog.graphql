type CatalogResponse {
  id: Int
  name: String!
  catalogId: Int
  subcatalogId: Int
}

type Query {
  getCatalogs: [CatalogResponse!]!
  getCatalog(id: Int!): CatalogResponse!
}

type Mutation {
  suscribeUser(userId: String!, planId: Int!): UserResponse!
}
